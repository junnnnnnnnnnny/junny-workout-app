rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 모든 데이터는 users/{uid} 아래에 있으며, 본인만 읽고 쓸 수 있다.
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;

      match /{subcollection}/{docId} {
        allow read, write: if request.auth != null && request.auth.uid == uid;
      }
    }

    // 전체 사용자가 함께 쓰는 공통 음식 DB. 누구나 읽을 수 있고, 새 항목 추가만 가능하며
    // (자신이 등록한 항목이어야 함) 기존 항목 수정/삭제는 클라이언트에서 할 수 없다.
    match /foods/{foodId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.createdBy == request.auth.uid;
      allow update, delete: if false;
    }

    // 전체 사용자가 함께 쓰는 공통 운동 DB. 누구나 읽을 수 있지만, 새 운동 등록은
    // 관리자 계정만 가능하다(음식 DB와 달리 아무나 추가하지 못하게 제한).
    match /exercises/{exerciseId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null
        && request.auth.uid == "46Wso3o8v9Z5kS8X2TrxNcfh3TK2"
        && request.resource.data.createdBy == request.auth.uid;
      allow update, delete: if false;
    }
  }
}
