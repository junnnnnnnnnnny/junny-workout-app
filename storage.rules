rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // 인바디 사진 등 사용자별 업로드 파일: users/{uid}/... 경로만 허용
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
